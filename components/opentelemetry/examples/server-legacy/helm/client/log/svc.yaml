# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: test-loki-logger
  namespace: default
  labels:
    app: test-loki-logger
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: test-loki-logger
