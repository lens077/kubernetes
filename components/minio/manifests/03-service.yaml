apiVersion: v1
kind: Service
metadata:
  name: minio-service          # 沿用既有命名（应用侧 S3 endpoint 已按这个配）
  namespace: minio
spec:
  type: ClusterIP
  selector: {app: minio}
  ports:
    - {name: api, port: 9000, targetPort: 9000}
    - {name: console, port: 9090, targetPort: 9090}
