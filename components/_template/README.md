# <组件>

> 解决方案文档模板。六节都要写满；重点是第 3 节「本集群取舍」——记录**为什么这么配**，
> 而不是抄一遍官方文档。

## 1. 定位

这个组件在本集群解决什么问题，谁依赖它，不装会怎样。

## 2. 上游最佳实践

官方推荐的生产配置要点（附调研来源链接）。只写与本集群相关的部分。

## 3. 本集群取舍

集群特性：单控制面 + 2 节点（node2 控制面 / node1 工作节点）、ARM64（Parallels VM，
Ubuntu 26.04，内核 7.0）、Cilium eBPF 完全替代 kube-proxy、OpenEBS LVM 本地卷
（`${SC_NAME}`，有节点绑定特性）、内存偏紧。

| 上游默认 | 本集群 | 原因 |
|---|---|---|
| | | |

## 4. 暴露方式

- Gateway 资源：`gateway/` 下哪些文件、挂在哪个 Gateway 的哪个 listener
- 访问地址：`https://<组件>.dev.test`
- 证书来源：cert-manager 的哪个 Issuer
- 集群内地址：`<svc>.<ns>.svc.cluster.local:<port>`

## 5. 验证

一条能证明「**真的通了**」的命令——打真数据再回查，不是 `kubectl get pod`。

```bash
# 例：写入一条数据 → 从另一端查回来
```

## 6. 踩坑

已知问题与修复。写清楚**现象 → 根因 → 修法**，便于以后 grep。
