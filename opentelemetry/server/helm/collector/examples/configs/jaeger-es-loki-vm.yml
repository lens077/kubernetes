apiVersion: opentelemetry.io/v1beta1
kind: OpenTelemetryCollector
metadata:
  name: otel
  namespace: observability
spec:
  mode: daemonset # DaemonSet 模式才能采集每台虚拟机的指标
  config:
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
          http:
            endpoint: 0.0.0.0:4318

      # host_metrics 接收器
      host_metrics:
        root_path: /hostfs # 指向容器内挂载的宿主机根路径
        collection_interval: 10s # 本地环境 10s 即可，降低 CPU 消耗
        scrapers:
          cpu:
          load:
          memory:
          disk:
          network:
          filesystem:
            exclude_mount_points:
              mount_points: ["/dev", "/proc", "/sys"]
              match_type: strict
    processors:
      # 批处理器
      batch:
        # 避免每一条数据都发一个网络请求，从而降低 CPU 消耗和网络负载。
        # 减少网络请求次数，提高传输效率, 到达该参数的值才打包发送
        send_batch_size: 800
        # 保证数据的实时性，防止在低流量时数据卡在内存里发不出去
        # 如果等待了设定的值还没攒满send_batch_size的值，不管有多少，直接强行发走
        timeout: 5s
      # 内存限制器
      memory_limiter:
        # 每隔 1 秒检查一次当前的内存占用情况。
        check_interval: 1s
        # 软限制（Soft Limit）为 设定值,单位MiB。
        limit_mib: 1000
        # 如果内存瞬间飙升超过 limit_mib + spike_limit_mib（即 设定的值,单位 MiB），会采取更激进的保护措施
        spike_limit_mib: 200
      k8s_attributes:
        extract:
          metadata:
            - k8s.pod.name
            - k8s.namespace.name
            - k8s.node.name
        passthrough: false

    exporters:
      # 把 trace 发送给 Jaeger 的 OTLP 端口
      otlp_grpc/jaeger:
        endpoint: jaeger.observability.svc:4317   # Jaeger 的 Service 名和端口
        tls:
          insecure: true   # 集群内通信一般无需 TLS

      # Metrics 导出到 VictoriaMetrics
      otlp_http/victoriametrics:
        endpoint: http://vm-single-victoria-metrics-single-server.victoriametrics.svc:8428/opentelemetry
        tls:
          insecure: true
        compression: gzip

      # Logs 导出到 Loki (注意：Loki 的 OTLP 端点通常为 /otlp/v1/logs, otel会自动添加/v1/logs后缀)
      otlp_http/loki:
        endpoint: http://loki.loki.svc.cluster.local:3100/otlp
        tls:
          insecure: true

    service:
      extensions: [health_check]
      pipelines:
        traces:
          receivers: [ otlp ]
          processors: [ memory_limiter, k8s_attributes, batch ]
          exporters: [ otlp_grpc/jaeger ]
        metrics:
          receivers: [ otlp,host_metrics ]
          # k8s_attributes 原来只挂在 traces/logs 上,metrics 漏了 —— 结果进
          # VictoriaMetrics 的指标没有 k8s.pod.name / namespace / node,
          # 「哪个 pod 在抖」这类问题只能靠 service.instance.id 猜。
          # host_metrics 是 collector 自产的、没有连接来源,processor 关联不上会
          # 原样放行,不影响它;真正补上标签的是应用经 OTLP 上报的那部分。
          processors: [ memory_limiter, k8s_attributes, batch ]
          exporters: [ otlp_http/victoriametrics ]
        logs:
          receivers: [ otlp ]
          processors: [ memory_limiter, k8s_attributes, batch ]
          exporters: [ otlp_http/loki ]

      telemetry:
        metrics:
          level: detailed
          readers:
            - pull:
                exporter:
                  prometheus:
                    host: 0.0.0.0
                    port: 8888

    extensions:
      health_check:
        endpoint: 0.0.0.0:13133

  # 注入宿主机根路径卷挂载，供 host_metrics 读取
  volumes:
    - name: hostfs
      hostPath:
        path: /
  volumeMounts:
    - name: hostfs
      mountPath: /hostfs
      readOnly: true
      mountPropagation: HostToContainer
