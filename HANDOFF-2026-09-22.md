# 交接文档：k1/k2/k3 重建（2026-09-22）

本文覆盖 2026-09-21 起的机房集群重建：旧 `node3`/`node4`/`node5` 三台主机重装并改名为
`k3`/`k1`/`k2`，IP 不变。集群、存储、CNPG、CDC、观测栈已经跑通；**公网入口（Pangolin/newt）
和业务数据恢复仍未完成**，接手后第一优先级是这两项。

真相源：本文只写结论与边界，运行态数字现查 `kubectl`。恢复过程的逐项记录见
[`RECOVERY-STATUS-2026-09-22.md`](RECOVERY-STATUS-2026-09-22.md)，安装器手顺见
[`bootstrap/README.md`](bootstrap/README.md)。

---

## 1. 主机与改名对照

| 现名 | 旧名 | IP | 角色 |
|---|---|---|---|
| `k1` | node4 | 10.10.21.161 | control-plane + 工作负载（污点已摘除） |
| `k2` | node5 | 10.10.21.162 | worker |
| `k3` | node3 | 10.10.21.163 | worker |

SSH 入口是 `root@211.144.221.229`，端口 `44161`/`44162`/`44163`，本机 `~/.ssh/config` 已配
`k1`/`k2`/`k3` 别名。

三台规格一致：4 vCPU / 7.3Gi 内存 / 150G 盘。`sda3` 已被 `ubuntu-vg/ubuntu-lv` 占满（VFree 0），
**没有可用的未分配尾部空间**，所以 OpenEBS 用的是安装器的 80G 稀疏 loopback
（`/var/openebs/openebs-lvm.img` + `openebs-lvm-loop.service`），不是裸盘。要扩容先扩 loopback 文件，
不要试图在线缩 root LV。

⚠️ 旧 `node3` 上的 Pigsty（PostgreSQL、VictoriaMetrics/Logs/Traces、vmalert、Alertmanager、
Gatus、Healthchecks、Bugsink、CDC Connect、Elasticsearch）**已随重装全部消失**。仓库里所有指向
`10.10.21.172`、`*-node3.apikv.com`、`es.apikv.com` 的东西都是历史，不要照着回填。

## 2. 集群现状

- Kubernetes `v1.36.4`，containerd `2.3.4`，runc `1.5.1`
- Cilium `1.20.1`：native routing + netkit，kube-proxy 已完全替代，Gateway API v1.6.1
- OpenEBS LVM LocalPV `4.5.1`：三台各一个 `openebs-vg`，默认 StorageClass `openebs-lvm`（xfs）
- LB-IPAM：共享 Gateway VIP `10.10.31.240/32`，默认池 `10.10.31.241-249`，L2 通告开启

`sudo bash bootstrap/start.sh --verify` 已全绿：节点健康、kube-proxy 替代、系统调优、
GracefulNodeShutdown、终态 Pod GC、PVC 读写冒烟、LB VIP 分配与编程、L2 租约。

**验收报告里「已装组件：无」是显示缺陷**：90 阶段只读本轮 `--only` 的选择状态，不扫描 live 集群。
组件实际装了哪些以 `helm list -A` 和 `kubectl get pods -A` 为准。

⚠️ 这个缺陷会**漏掉真实故障**，不只是显示不好看。2026-09-22 下午发现：共享 Gateway
`default/cilium-gateway` 一直没装，14 条 HTTPRoute 全部 `parentRef` 到一个不存在的
Gateway、`status` 为空——**集群没有任何 HTTP 入口**，而所有 Pod 都是 Running，
`--verify` 也全绿（`90-verify.sh` 只在本轮选了 gateway 时才检查它）。
已跑 `components/gateway/install.sh` 补齐，VIP `10.10.31.240` 已 Programmed。
教训：**「Pod 全 Running」和「--verify 全绿」都不能证明入口可用**，要按 hostname 实测。

已部署的 Helm release（截至 2026-09-22 实测）：cilium、openebs、cert-manager、external-secrets、
spegel、cnpg、strimzi、vm-single、vl、vt、vmalert、alertmanager、otel、vector、grafana。
另有自写 manifest 部署的 elasticsearch、gatus、healthchecks、alert-bridge。

## 3. 数据面：CNPG + CDC

### 3.1 CNPG

`pg-main` 是 CloudNativePG 的 `Cluster` 名，不是库名。当前单实例：

```text
Cluster:  pg-main            (ns postgresql)
实例:     pg-main-1
写入:     pg-main-rw.postgresql.svc.cluster.local:5432
库:       ecommerce          (Database CR)
凭据:     Secret pg-main-app (CNPG 自动生成, 不要手工改)
```

### 3.2 建表来自 Go 迁移，不是 SQL 备份

项目没有顶层 `data/` 或 `examples/` 目录。真实布局是每个服务自带：

```text
backend/services/<svc>/internal/data/migrations/   # goose 注解 SQL, 同时是 sqlc 的 schema 输入
backend/services/<svc>/internal/data/seeds/        # 幂等种子, goose no-versioning
```

入口是 `backend/tools/dbmigrate`（goose v3 内嵌，不需要外部 CLI）。本次已对 9 个服务执行
`up` + `seed`：address、behavior、cart、inventory、merchant、order、payment、product、user。

**仓库自带的 `make migrate-cnpg-up` 在 Mac 上跑不通**：它建一个临时 LoadBalancer 然后把 DSN 写成
那个 VIP，而 Mac 只能经 SSH 隧道到达集群。实际可用的做法是自己开隧道再调同一个 Go 入口：

```bash
# 1) 建临时 LB（脚本里那段 YAML），拿到 VIP
# 2) ssh -N -L 16432:<VIP>:5432 k1
# 3) 在 backend/ 下：
export DB_URI="postgres://app:<pg-main-app 的密码>@127.0.0.1:16432/ecommerce?sslmode=require"
go run ./tools/dbmigrate -svc all up
go run ./tools/dbmigrate -svc all seed
# 4) 删掉临时 LB 和隧道
```

### 3.3 CDC 链路

```text
CNPG pg-main → Debezium PostgreSQL source → Kafka my-cluster → Kafka Connect → Elasticsearch
```

两个 connector 当前 `Ready=True`、task `RUNNING`。重建时踩到并已修的三点：

1. source 的 `database.hostname` 原本写死旧 node3 的 `10.10.21.172`，已改为 `pg-main-rw.postgresql.svc.cluster.local`。
2. CNPG 的 `app` 角色默认没有 `REPLICATION`，Debezium 建 slot 时报
   `permission denied to start WAL sender`。已执行 `ALTER ROLE app REPLICATION`。
3. `publication.autocreate.mode: disabled`，所以 `ecommerce_cdc` publication 必须手工建。已建。

Elasticsearch sink 还要求目标 alias 预先存在（`external.resource.usage: ALIAS_INDEX`），
7 个 `<alias>_v1` 索引与 write alias 已建好。

⚠️ **CDC 目前只证明了链路可用，没有证明业务数据流转**。`ecommerce` 库里现在只有迁移建的表和
种子，没有旧业务数据。真实 dump 恢复后必须重新端到端验一次。

## 4. Elasticsearch 与 IK

镜像改用公开的 `docker.elastic.co/elasticsearch/elasticsearch:9.4.5`，**不再依赖 TCR 私有镜像和
`tcr-pull-secret`**（那个 secret 在新集群里不存在，原来的 `ccr.ccs.tencentyun.com/...` 镜像拉不动）。

IK 由 initContainer 装进共享插件卷，不需要自建镜像：

1. 从 `https://get.infini.cloud/elasticsearch/analysis-ik/9.4.5` 下载
2. 校验 SHA-512（值取自 `docker-postgres-kafka-es-streaming-pipeline/ecommerce-cdc/Dockerfile.elasticsearch`）
3. `elasticsearch-plugin install`
4. **额外从 zip 里解出 `config/` 到 `/usr/share/elasticsearch/config/analysis-ik/`**

第 4 步是必须的：`elasticsearch-plugin` 装完不会把词典留在插件目录下，IK 启动时读
`config/analysis-ik/IKAnalyzer.cfg.xml` 会 `FileNotFoundException`，表现是插件 loaded 但
`_analyze` 返回 500。已用中文实测：`ik_smart` 对「灯市商品」正确切成 `灯市`/`商品`。

若要改回自建镜像（比如要带自定义词典），保留第 4 步的语义。

## 5. 观测栈

全部回到集群内，旧 node3 Pigsty 的远端写入已关闭（`components/opentelemetry/component.env` 的
三个 `REMOTE_*_URL` 已清空）：

| 信号 | 后端 | 采集 |
|---|---|---|
| metrics | VictoriaMetrics (`victoriametrics` ns) | OTel Collector |
| logs | VictoriaLogs (`logging` ns) | Vector DaemonSet 直写 + OTel（应用侧 OTLP / K8s Event）|
| traces | VictoriaTraces (`observability` ns) | OTel Collector |

告警链路：vmalert（5 个规则文件）→ Alertmanager → alert-bridge → ntfy。已实测 vmalert 能查到
Alertmanager 里的 active alerts，Watchdog 心跳在。

Grafana `13.2.2`（当前 chart 已升级，sidecar `2.11.2`）已部署，`/api/health` 返回 `database: ok`，
四个数据源按集群实际后端预置：VictoriaMetrics（默认）、VictoriaLogs、VictoriaTraces、Alertmanager。
admin 口令走 `get_cred` 降级路径写进 Secret `grafana-admin`（OpenBao 未启用，ESO 路径不可用）。

⚠️ Grafana 只能经共享 Gateway 访问（`grafana.dev.test`，ClusterIP + HTTPRoute），
**公网入口要等 newt 恢复**。

## 6. 网络与镜像的现实约束

这几条是反复踩出来的，动手前先看：

- **Helm 仓库直连基本不通**。VictoriaMetrics / prometheus-community / grafana / open-telemetry /
  vector 的 index 在机房侧都会超时。可用路径：本机 `zsh` 的 `proxy` 下载 chart tgz →
  `scp` 到节点 `/var/cache/k8s-installer/charts/`，同时把 `index.yaml` 放到
  `/root/.cache/helm/repository/<repo>-index.yaml`。`components/_lib/env.sh` 已改成
  「缓存里有对应版本的 tgz 就不刷新远端仓库」，所以这条路是受支持的，不是绕过。
  chart 的 GitHub 下载走 `GITHUB_PROXY`（`https://ghfast.top`）。
- **镜像优先走 `bootstrap/files/certs.d` 里的 mirror**，那是仓库自带的完整 registry 配置
  （含 Spegel P2P `:30021` 和一堆国内站）。实测：Docker Hub 官方直连会在大层上 `unexpected EOF`；
  多数国内 mirror 对非公开路径返回 401/403；`quay.io` 对 `openebs/*` 可用、对 `grafana/*` 和
  `timberio/*` 401。**节点到不了本机的 proxy**（`192.168.3.220:7890`），别指望。
- OpenEBS 的两个镜像已在 `bootstrap/scripts/70-storage.sh` 里固定覆盖为 Quay 同源：
  `quay.io/openebs/provisioner-localpv:4.5.1`、`quay.io/openebs/lvm-driver:1.9.1`。
- 大镜像（Grafana、Elasticsearch、Vector）首次拉取要 5-15 分钟，`ImagePullBackOff` 不一定是
  永久失败，先看 `kubectl describe` 里的 `Pulling` 时间再决定是否换源。

## 7. 未完成项（按优先级）

### 7.1 Pangolin / newt 公网入口 —— 已完成迁移

这是当前最大的缺口。现场事实（2026-09-22 下午复核并更新）：

- node1 上是 **Docker 版 Pangolin 控制面**：`fosrl/pangolin:1.23.0` + `traefik:v3.7.12` +
  `fosrl/gerbil:1.5.2`，compose 在 `/home/docker/pangolin/`。
  （升级前是 1.22.2 / 1.5.0；升级备份在 `/root/pangolin-backups/<时间戳>/`。）
- node1 上**这套 Docker 实例里**没有 newt 容器，也没有 `newt.service` / 二进制。
  面板里那个 `node1-local` 站点 `type=local`，不是 newt 站点——**它不证明 node1 跑着 newt**，
  `gerbil` 也不是 newt。newt 是客户端，装在被暴露的那一侧。
  （注意措辞：这是对当前实例的观察，不是「Pangolin 服务端不可能同机跑 newt」这种普遍规则。）
- Pangolin 里旧 `node3`(siteId 7)、`node5`(9)、`node4`(10) site 已删除；资源已迁到新 `k8s-cluster` siteId 11。
- 新集群里仍**没有** newt Pod。`bootstrap/config.hosting.env` 里 `ADDON_NEWT` 其实已经是
  `true`（本文早前写的 false 已过期），缺的是**站点凭据**，不是开关。
- 仍在线的三个 newt site：`node2`(5)、`mac`(6)、`node0`(8)。node0/node2 的 newt 已升到
  `1.17.0`（node0 是 Docker，已从未钉版本的 `fosrl/newt` 钉到 `:1.17.0`；node2 是
  `/usr/local/bin/newt` + systemd，旧二进制备份为 `newt.bak-1.16.0`）。

所以「升级 node1 的 newt」这个说法不成立：node1 是 Pangolin server，集群入口由 k8s-cluster site 承载。
旧三个 site 的资源已迁移完成并删除；当前应把 k1/k2/k3 site 视为主机级备用入口，不要再给同一个
k8s-cluster newtId 启多个 Pod。正确迁移顺序（已执行记录）：

1. 在 `https://pangolin.apikv.com` 新建一个集群 newt site，拿 `newt-id` / `newt-secret`
2. 放到 k1 的 `/var/lib/k8s-installer/creds/newt-{id,secret}`
3. `ADDON_NEWT="true"` → `bash components/newt/install.sh`，确认 site 变 online
4. 把资源 target 从旧 site 迁到新 site（见下表）
5. 逐个验证公网入口
6. 确认全绿后再删 `node3`/`node4`/`node5` 三个 site

> ✅ **以上六步已于 2026-09-22 下午全部完成**，本节保留作为手顺参考。当前站点拓扑：
>
> | siteId | 名称 | 类型 | 用途 |
> |---:|---|---|---|
> | 1 | `node1-local` | local | node1 自身的 Docker 服务 |
> | 5 / 6 / 8 | `node2` / `mac` / `node0` | newt | 原有，未改动（node0/node2 客户端已升 1.17.0）|
> | 11 | `k8s-cluster` | newt | **集群入口**，Pod 在 `pangolin` ns，承载全部已迁移资源 |
> | 12 / 13 / 14 | `k1` / `k2` / `k3` | newt | 三台节点的**主机级**隧道，给日后直接跑在节点上的非 k8s（Docker）组件用，target 走 `127.0.0.1:<port>`；装法同 node2（`/usr/local/bin/newt` + systemd，凭据在 `/home/docker/newt/newt.env`）|
>
> 为什么集群只用**一个** site：所有 k8s 资源的 target 都是共享 Gateway VIP，
> 集群内一个 newt Pod 就能全部路由到；多副本共用同一 newtId 会互相顶替（manifest 里有注释）。
> 冗余由 Gateway VIP + k8s 调度提供，不再靠 Pangolin 层的双 target。

### 迁移对照表（2026-09-22 下午按 live 集群逐条核对，已完成）

先说一个**容易踩的结构事实**：`node4`(10) 和 `node5`(9) 挂的是**同一批 resourceId**，
每个资源在两个 site 各有一条 target（双活冗余）；`node3`(7) 挂的是另一批独立资源。
所以删 site 会连带删掉它的 target——**必须先给保留的资源补上 k 站点 target，再删 site**，
否则资源会变成「有域名、没后端」。

判定口径：以 `kubectl get svc -A` / `helm list -A` 的**实际部署**为准，不看旧文档。

**A. 保留并迁移**（组件已部署且有对外 HTTPRoute；target 统一改 `10.10.31.240:443`，
method `https`，并把 Host/`tlsServerName` 设成括号里的内部 hostname）：

| 旧 site | rid | 资源 | 公网域名 | 新 target |
|---|---:|---|---|---|
| k8s-cluster | 20 | grafana | `grafana.apikv.com` | VIP:443（`grafana.dev.test`） |
| k8s-cluster | 21 | metrics | `metrics.apikv.com` | VIP:443（`metrics.dev.test`） |
| k8s-cluster | 23 | traces | `traces.apikv.com` | VIP:443（`traces.dev.test`） |
| k8s-cluster | 24 | vmalert | `vmalert.apikv.com` | VIP:443（`vmalert.dev.test`） |
| k8s-cluster | 25 | alerts | `alerts.apikv.com` | VIP:443（`alerts.dev.test`） |
| node4/5 | 56 | healthchecks | `hc.apikv.com` | VIP:443（`hc.apikv.com`） |
| node3 | 26 | pg（TCP `:30001`） | — | `10.10.31.241:5432` |

`pg` 这条是本次**新发现**：交接文档原先写「CNPG 只有 ClusterIP，要公网 PG 需另建
passthrough」——实际上 `postgresql/pg-passthrough-gateway` 已经存在并拿到 `10.10.31.241`，
直接指过去即可，不用新建。

资源 20/23/24/25 的名字还带 `node3-` 前缀，那是旧主机名。**先迁移、后改名**，
同一轮里既换 target 又改 niceId 容易把自己绕晕。

**B. 直接删除**（组件在新集群里不存在，`kubectl get ns/svc` 已确认）：

| 旧 site | rid | 资源 | 为什么删 |
|---|---:|---|---|
| node3 | 27 | redis（TCP `:30002`） | 旧 node1/PG-side Redis，不是 ecommerce 缓存；Dragonfly 已迁入集群，旧资源可删 |
| node3 | 48 / 49 | Lyrapass / 云账户lyrapass | 对应组件未部署 |
| node4/5 | 53 | redis-dev（TCP `:30005`） | 目标 LB `10.10.31.243` 已不存在 |
| node4/5 | 29 / 37 | node3-minio / node3-silo | MinIO 未部署 |
| node4/5 | 35 | bugsink | 未部署 |
| node4/5 | 40 / 58 | argocd / argocd-api | ArgoCD Web UI 已部署，但旧 Pangolin 资源未迁移；Web UI 新 host 为 `argocd.dev.test`，CLI/gRPC 入口另行设计 |
| node4/5 | 41 / 52 | consul / consul-dev | ns 存在但无任何 Service |
| node4/5 | 46 | qqbot | 未部署 |
| node4/5 | 57 | umami | 未部署 |
| node4/5 | 3 / 4 | config-center-web / config-center-api | 未部署（control-tower 侧） |
| node4/5 | 14 / 15 | gateway / shop | 未部署（ecommerce 业务） |

⚠️ 最后四条（3/4/14/15）是 **ecommerce 主业务域名**，组件确实没装，按「未安装即删」处理。
但它们和上面那些「已废弃」的组件性质不同——是**尚未部署**而非不再需要。
业务上线时要重新建资源，域名与 target 约定记在这里以免丢：
`config.apikv.com` / `config-api.apikv.com` / `gateway.apikv.com` / `shop.apikv.com`，
四条原本都指 `10.10.31.240:443`（即共享 Gateway），恢复时按 A 类写法重建即可。

**C. 组件已装、但仓库刻意不对外暴露 → 也删**（2026-09-22 与用户确认）：

| 旧 site | rid | 资源 | 说明 |
|---|---:|---|---|
| node3 | 22 | node3-logs | VictoriaLogs 已部署，但 `EXPOSE=none` 且无 gateway 目录；日志查询走 Grafana 的 VL 数据源，不单独暴露 VL 自带 UI |
| node3 | 30 | node3-otlp | OTel Collector 已部署，但 OTLP 设计上只在集群内；原 Bearer 鉴权未迁移，无鉴权的公网写入口等于任人灌数据 |

**D. 不动**：`node1-local`(1)、`node2`(5)、`mac`(6)、`node0`(8) 四个站点及其资源与本次迁移无关。

#### 迁移过程中发现、且已修的三件事

1. **不要把 `pg` 的 target 指向 `10.10.31.241:5432`**。那是 `pg-passthrough-gateway`
   的 **TLS Passthrough** 监听器，只认直接 ClientHello + SNI；标准 PostgreSQL 客户端先发
   明文 SSLRequest，Envoy 会回 `HTTP/1.1 400 Bad Request`（实测）。
   正确写法是指向 `pg-main-rw` 的 ClusterIP:5432，让 PostgreSQL 自己完成 TLS 协商。
   本文早前那句「要公网 PG 需另建 passthrough」按此更正。
2. **`connect-rest-netpol.yaml` 在仓库里但没 apply 到集群**。Strimzi 生成的
   NetworkPolicy 只放行 operator，导致 gatus 从 `ops` 探 Connect REST `:8083` 恒 10s 超时，
   而 `kubectl get kafkaconnector` 却显示 `Ready=True`——**两个信号矛盾时以探针为准**，
   它测的是真实网络路径。apply 后两条 CDC 探针立即转绿。
   这多半也是本文 §7.4 里 `EcommerceNetworkPolicyDeniedBurst` 的来源之一。
3. **迁移后 `sso=1` 的资源匿名访问恒 401**，这是 Pangolin 侧既有策略（1.23 的
   `resourcePolicies` 表），不是迁移失败。判断方法：401 响应里**没有** `server: envoy` 头，
   说明请求压根没到集群网关。`traces` / `vmalert` / `alerts` / `hc` 四个属于此类。

`components/gatus/endpoints.yaml` 的 `public-edge` 组已随本次迁移重写并重新部署：
删掉了指向已删资源的五条（gateway / mall / config-center-web / config-center-api / bugsink-edge），
保留 casdoor + grafana-edge，新增 metrics-edge 与 pg-edge。`cluster-origin/bugsink`
也一并注释掉（组件未部署，恒红刷 ntfy）。当前 **16 条探针全绿**。

选判据时注意：**不要拿 401 当「活着」**。SSO 保护的资源匿名恒 401，只能证明 Pangolin 边缘在，
证明不了集群后端，反而会掩盖后端故障；那几个后端已由 `cluster-origin` 组覆盖。

### 7.2 业务数据恢复 —— 已完成（2026-09-23）

`backup/pigsty-node3-20260921/postgres/` 里只有权限 SQL，**真实 dump 在仓库外**：
`~/lens077/backs/node3/pigsty-node3-2026-09-03/raw/_data/ecommerce.pgdump`（custom 格式，2026-09-05 归档，
pg_dump 18.6）。同目录的 `openfga.pgdump` 是空的（0 store / 0 model / 0 tuple），没有可恢复内容。

恢复方式是**先隔离比对、再 additive 合入**，没有覆盖 live 库：

1. 流式 `pg_restore` 到 CNPG 上的临时库 `ecommerce_restore`（排除 `pg_repack` 扩展，CNPG 镜像没有它）。
2. 逐表比对：备份里的商品投影（spus 7 / skus 13 / sale_detail 21 / search_catalog 7）与 live **完全一致**——
   都来自同一套 Go seed；订单/用户/购物车在备份里是 0 行，live 反而多 3 条 seed 订单。
   所以业务表**没有任何 live 缺失的数据**，不动。
3. 备份里 live 没有的只有 `config` schema（Config Center 的 20 条 entry / 16 个 machine token / 57 个 revision，
   10 个服务的 `dev` + gateway 的 `dev/pre`）——用 `pg_restore -n config` 合入 live `ecommerce`，
   owner 改为 `app`。Config Center 目前没在新集群上跑；重新部署时直接接 `pg-main`，旧值由
   `tools/config-center-harvest.sh` 刷新。
4. 临时库已 drop，Pod 里的 dump 已删。

CDC 端到端用 live 真实 SKU 做了可逆验证：改 `products.skus id=1` 价格 → Debezium source RUNNING →
`ecommerce_cdc.products.skus` 出新消息 → ES `ecommerce_products_skus_v1/_doc/1` 的 `updated_at` 跟着变；随后改回原价。

### 7.3 其它组件 —— 本轮已补齐七个（2026-09-22/23）

| 组件 | 版本 | 验收证据（都是行为，不是 Pod Running） |
|---|---|---|
| OpenBao + ESO | 0.29.2 / ESO 2.10.0 | `ClusterSecretStore openbao` Ready；`tools/openbao-seed.sh dragonfly grafana healthchecks` 取现值播种；三个组件 `install.sh` 重跑后 Secret 归属 `ExternalSecret`、值哈希前后一致、`SecretSynced`；Dragonfly 用 ESO 管的密码 `PONG`/错密码 `NOAUTH` |
| OpenFGA | 0.3.15 / v1.21.0 | store = CNPG `pg-main` 独立库 `openfga`（Database/DatabaseRole CR）；`examples/smoke.sh` 建 store→写模型→写 tuple→check `allowed=true` |
| KEDA | 2.20.2 | `examples/cron-demo.yaml`：ScaledObject Ready/Active，Deployment 0→2/2，KEDA 生成的 HPA 存在 |
| Kyverno | 3.9.1 / v1.19.1 | 两条集群级 Audit 策略；`examples/smoke.sh` 违规 Pod 放行 + PolicyReport 记两条 fail；业务 `verifyImages` 见 ecommerce 仓 `infrastructure/kyverno/` |
| Tetragon | 1.7.1 | `verify.sh`：k1/k2/k3 三 agent、BTF、唯一 audit-only 策略；`examples/cnp-smoke.sh`：CNP 放行 client→echo、拒绝未授权 egress 且 Hubble 有 `Policy denied` |
| VPA | 0.12.0 / 1.7.1 | recommender + admission webhook，**updater 已关**（2026-09-23 拍板：只算建议、不驱逐；`observability/grafana` VPA `Off` 已出 Target≈11m/523Mi，改 requests 前须人工确认）；webhook 证书走 cert-manager；`examples/smoke.sh`：新 Pod 带 `vpaObservedContainers`，`Initial` 模式重建 Pod 被注入推荐值，且断言集群其余 VPA 全为 Off/RequestsOnly |
| Argo Rollouts | 2.43.2 / v1.10.0 | `examples/rollout-demo.yaml` 改镜像：`Progressing/step0 → Paused/step1 → Progressing/step2 → Healthy`，`stableRS == currentPodHash` |

仍未部署：Harbor、MinIO、Consul、kured、trust-manager（不在本轮目标内）。

#### OpenBao 的一个待决策项：重启即 sealed

当前是 shamir（1 share / 1 threshold，测试级）+ file 存储的 standalone。**Pod 一重启就回到 sealed**，
ESO 立刻 `SecretSyncedError`，组件 install.sh 只能走 `get_cred` 降级。已加 Gatus 探针 `openbao-unsealed`
（`/v1/sys/health` 非 200 即告警），恢复动作是 `bash components/openbao/examples/unseal.sh`。

要消除人工解封，OpenBao 2.6 有 `seal "static"`（32 字节密钥从文件读，自动解封），但密钥会和数据 PVC
在同一个 K8s 信任域里，且需要做一次 seal migration——这是安全取舍，没有在自动轮次里替你决定。
另两个选项：VPS Vault 做 transit auto-unseal（外部依赖，AppRole 凭据当前没有）；保持手动。

#### 镜像与 chart 的两条新经验

- **`reg.kyverno.io` 等上游在机房拉不动 → 走 TCR 镜像仓**。Mac 是 arm64，`docker pull`/`docker push` 都必须带
  `--platform linux/amd64`，否则推上去的是 arm64 层，节点上 `exec format error` CrashLoop（Kyverno 五个控制器全中过）。
  拉取凭据放 `/var/lib/k8s-installer/creds/tcr-dockerconfig.json`，`_lib/env.sh` 的 `tcr_pull_secret_ensure` 物化。
- **手工 scp 进 `/var/cache/k8s-installer/charts/` 的 chart 之前装不上**：`helm_install_component` 只跳过了
  `repo add` 却仍把 `repo/chart` 交给 helm。已修（命中缓存直接传 tgz 路径），回归测试
  `bootstrap/tests/test-helm-chart-cache.sh`。所有组件 install.sh 现在都钉了 `--version`，否则永远走不了缓存。

### 7.5 remote-dev 闭环（2026-09-23）

三条 L4 入口 + ArgoCD 公网入口都在 Pangolin 上建好并做了**协议级**验证（不是状态码）：

| 入口 | Pangolin | 后端 | 证据 |
|---|---|---|---|
| PostgreSQL | `pg-dev.apikv.com:30001`（rid 26） | `pg-main-rw` ClusterIP:5432 | `app` TLS 登录，`ecommerce` 33 表 |
| Dragonfly | `redis-dev.apikv.com:30005`（rid 59） | `10.10.31.242:6379` | CA+SNI 校验、`PING/SET/GET`、错密码拒绝 |
| Kafka | `kafka-dev.apikv.com:30004`（rid 61，**本轮新建**） | `10.10.31.243:9094`（`cdc/kafka-external-gateway.yaml`）→ Strimzi external listener（`cluster-ip`，SASL_SSL/SCRAM，`KafkaUser remote-dev`） | metadata 里 broker 0 advertised=公网地址、公网 produce→consume 往返、错密码 `Authentication failed` |
| ArgoCD Web UI | `argocd.apikv.com`（rid 60，**本轮新建**，SSO 关） | VIP:443，Host `argocd.dev.test` | admin 登录拿 JWT、`/api/v1/applications`、`/api/v1/clusters=in-cluster` |

用户报的 **`argocd.dev.test` 502** 不是集群故障：`*.dev.test` 只在 LAN 可解析，Mac 的 Firefox 走系统代理，
代理解析不了就回 502。集群侧 `curl --resolve argocd.dev.test:443:10.10.31.240` 一直是 200。Mac 用 `argocd.apikv.com`。

SSO 保护的四个资源（traces/vmalert/alerts/hc）用 Pangolin **resource access token**（`POST /resource/{id}/access-token`，
15 分钟，用完已 DELETE）过边缘后拿到真实业务内容：vmalert 6 组 23 条规则、Alertmanager 活跃 `Watchdog`、
VictoriaTraces 应答 JSON（services 为空——业务服务还没跑，是事实不是故障）、Healthchecks 经应用 read-only API key 列出 1 个 check。
Firefox 扩展只对 `pangolin.apikv.com` 有 host 权限，读不了其它域名响应体，所以走 token 而不是页面快照。

**踩到一个真事故**：给 Kafka 加 external listener 让 broker 滚动，Connect rebalance 重启 Debezium task，
它拿 Connect offsets 里的 LSN 续流，但复制槽 `restart_lsn` 已推进到更后——task FAILED、connector 仍 RUNNING、
增量静默丢失（写一行 spus，topic offset 不动）。修法：`snapshot.mode: initial → when_needed`，task 自愈重快照
（spus 7→14）后续流；随后真实行往返（改名→ES v13 带标记→改回→topic 14→15→ES v14 还原，`search_catalog`
投影 topic 43→51、ES v50）。教训沉淀在 ecommerce 仓
`context/project/ecommerce/events/experience/debezium-offset-behind-slot-after-broker-roll.md`。
**改 Kafka/Connect 后必看 `tasks[0].state`，Ready=True 和 connector.state 都不看 task**——Gatus `cdc-source-task` 是唯一先红的信号。

**Config Center 已部署到新集群（2026-09-23）**，只差一个人工步骤。ns `config-center`：`control-tower-config:0.2.11` +
`config-web:0.2.15`，TCR 拉取 Secret `tcr-pull`（来自 `creds/tcr-dockerconfig.json`）；bootstrap `config.yaml` 按 control-tower
`services/config/configs/pre.yaml` 渲染成新集群地址——PG `pg-main-rw.postgresql.svc:5432`（`verify-full`，CA=`pg-main-ca`）、
Redis `dragonfly.dragonfly.svc:6379`（TLS，CA=`global-root-ca`）、指标查询集群内 VM；`casdoor.pem` 从 Casdoor 公开 JWKS 的
`x5c`（kid `lens`）导出，不需要管理员。启动日志：`database connected successfully to pg-main-rw`、goose 迁到 v3、
`redis connected successfully {dragonfly.dragonfly.svc}`；恢复进来的 `config` schema 数据完好（20 entry / 16 token / 57 revision）。
公网：Pangolin `config.apikv.com`(rid 62) / `config-api.apikv.com`(rid 63) → VIP:443，Host `config(-api).dev.test`，SSO 关；
web 标题「配置中心」、API `/healthz` 200、匿名 `ListKeys` 是应用自己的 401。`config-center-iam` 现在是**占位**
Secret（server 不读它，只有 configctl 用）。

**已闭环（2026-09-23 晚）。** 用户把 Casdoor 账号放到 k1 `/root/.casdoor-login` 后：
`ENVIRONMENT=dev tools/config-center-operator-token.sh` 签出 operator token（Secret `config-center-operator-dev`，自检列出 16 枚 token）；
`tools/openbao-seed.sh casdoor` 把 Casdoor client_secret/certificate 从现值反向抽进 OpenBao、ESO 物化 `external/casdoor`、
非机密 `client_id/organization/application` 写进 `overrides.yaml`；`config-center-harvest.sh --env dev`（策略 pangolin）
把 10 个服务的 `dev/bootstrap.yaml` 各改 7 处（PG→`pg-dev.apikv.com:30001`、Redis→`redis-dev.apikv.com:30005`、两个 CA、两个密码），
selector Secret `ecommerce/ecommerce-config-source-dev` 同步 10 枚 service token。search 额外拿到 ES：新建只读角色
`ecommerce-search-read` + API key（Secret `elasticsearch/search-api-key`，`install.sh` 幂等签发）、HTTPRoute `es.dev.test`、
Pangolin `es-dev.apikv.com`(rid 64, SSO 关)——公网只读 key 搜 `iphone` 命中、写 403、匿名 401。
Mac 侧证据：`config-api.apikv.com` 用 cart 的 service token 拿到 v6 bootstrap；三个 raw 口 TCP 可达；`redis-dev:30005` 用集群
根 CA 验证 `Verify return code: 0`；`kafka-dev:30004` broker 证书 SAN 含公网名。

顺手清掉的两处漂移：`_external/{postgres,redis,otlp,elasticsearch}-node3` 改 `DEFAULT_ENABLED=false` 并在 hosting env 显式关
（之前 `_external/apply.sh` 还在等它们的 ESO 物化超时）；新增契约字段 `REMOTE_POLICY=keep`（`opentelemetry` 用）——公网 OTLP
写入口没鉴权就是任人灌数据，harvest 在 pangolin 策略下对 otlp 块保持原值而不是 die，别拿集群 DNS 冒充 `REMOTE_HOST` 绕过。
Consul 块同样保持原值（没有启用的提供方，Consul 集群版未装）。

签 token 的既有手顺（轮换时复用）：
```bash
ssh k1 'umask 077; printf "%s\n%s\n" "<Casdoor 管理员用户名>" "<密码>" > /root/.casdoor-login'
ssh k1 'cd /root/kubernetes && ENVIRONMENT=dev bash tools/config-center-operator-token.sh'   # → Secret config-center/config-center-operator-dev
ssh k1 'cd /root/kubernetes && ADMIN_TOKEN_SECRET=config-center/config-center-operator-dev:token ENV=dev bash tools/config-center-harvest.sh --dry-run'
ssh k1 'cd /root/kubernetes && ADMIN_TOKEN_SECRET=config-center/config-center-operator-dev:token ENV=dev bash tools/config-center-harvest.sh'
```
harvest 的前置已就绪：schema 已同步到 k1 `/var/lib/k8s-installer/config-center/schemas/`，dry-run 已把提供方解析成
`postgres←postgres, redis←dragonfly, otlp←opentelemetry, casdoor←casdoor`（策略 `pangolin`），停在「没有管理 token 读不了现值」。
签完 token 跑上面两条 harvest，10 个服务的 `dev` bootstrap 就指向 `pg-dev:30001 / redis-dev:30005`，Mac `make dev` 闭环。

### 7.4 待查的真实告警

`EcommerceNetworkPolicyDeniedBurst` 当前 firing，5 分钟内 `POLICY_DENIED` 约 220 次，
只带 `protocol=TCP` 标签（Hubble 的 drop 指标没带 source/destination 维度）。
要定位得走 Hubble Relay 查 DROPPED flow。**不要为了消警调阈值或放宽 CIDR。**

Kafka / Connect / Vector 都带 NetworkPolicy，重建后新 Pod 网段与旧策略的匹配是首要怀疑方向。

## 8. 本轮改过的文件

### 安装器缺陷修复

| 文件 | 改了什么 | 为什么 |
|---|---|---|
| `bootstrap/lib/common.sh` | `terminal_pod_count()` 的 `.items` 加空值兜底 | 空集群时 `jq: Cannot iterate over null` 打断 50 阶段 |
| `bootstrap/scripts/70-storage.sh` | OpenEBS 两个镜像覆盖为 Quay | Docker Hub 上这两个镜像稳定 EOF |
| `components/_lib/env.sh` | 缓存里有对应版本 tgz 就跳过 `helm repo add` | 五个 Helm 仓库直连超时会卡死安装 |
| `components/grafana/install.sh` | 补 `--version "$CHART_VERSION"` | 不传版本就走不到缓存分支 |

### 组件配置（从旧环境切到新集群）

- `components/elasticsearch/manifests/01-statefulset.yaml`：公开镜像 + `ELASTIC_PASSWORD` Secret 注入 + IK initContainer
- `components/kafka/cdc/ecommerce-postgres-source.yaml`：target 改 CNPG
- `components/opentelemetry/component.env`：清空三个 `REMOTE_*_URL`
- `components/opentelemetry/values.yaml`：删除 `vector-security` scrape job
- `components/vector/values.yaml`：日志 sink 改集群内 VictoriaLogs
- `components/grafana/component.env`：钉 chart `10.5.15`
- `components/umami/*`：DB 从旧 node3 Pigsty 改为 CNPG
- `components/gatus/endpoints.yaml`：PG 探针改 CNPG，观测探针改集群内 VM，删除未部署 hostmetrics 探针；已重跑并验证
- `components/harbor/values.yaml`：nodeSelector `node5` → `k2`
- `components/newt/component.env`：注明旧 site 已失效、必须新建
- `components/dragonfly/`：新增标准组件契约，Dragonfly v1.39.0、TLS+AUTH、PVC、TCPRoute
- ecommerce `infrastructure/argocd/`：ArgoCD chart 10.9.2，Web UI HTTPRoute 改为 `argocd.dev.test`；原生 CLI gRPC 不经普通 HTTPS HTTPRoute
- `components/_external/{postgres,otlp,redis,elasticsearch}-node3/component.env`：全部标记已退役
- `components/vmalert/rules/ecommerce-k8s.yml`：数据来源说明改集群内 VM

### 配置与文档

- `bootstrap/config.hosting.env`：节点名/SANs/hosts 改 k1/k2/k3，启用 CNPG + 观测栈，
  `CILIUM_OPERATOR_REPLICAS=2`，`UMAMI_DB_ENDPOINT` 改 CNPG，关掉恢复期不需要的组件
- `bootstrap/config.env`：`CC_PROVIDERS` 从三个 node3 外部提供方改为集群内组件
- `bootstrap/README.md`、`bootstrap/CILIUM.md`、`bootstrap/tests/test-lb-config.sh`：旧主机名改 k1/k2/k3
- `README.md`、`OBSERVABILITY-INTEGRATION.md`、`components/elasticsearch/README.md`
- 新增 `RECOVERY-STATUS-2026-09-22.md` 与本文
- 删除 `bootstrap/node3-worker.sh`（未跟踪，内含写死的过期 join token，不能用）
- 删除 `PIGSTY-HARVEST-2026-09-03.md`、`RESTORE-RUNBOOK-2026-09-04.md`（按要求）

### 2026-09-22 下午追加改动

| 文件 | 改了什么 |
|---|---|
| `components/grafana/component.env` | chart 从已 deprecated 的 `grafana/grafana` 换到 `grafana-community/grafana` `13.2.5`（appVersion `13.2.2`）|
| `components/grafana/values.yaml` | `deploymentStrategy: Recreate`（RWO 本地卷升级死锁）、补 `resources.requests`、`image.tag` 去掉 `-distroless`、`grafana.ini` 补 `root_url`/`cookie_secure`/关闭两个更新检查 |
| `components/newt/manifests/01-deployment.yaml` | 镜像 `1.15.0` → `1.17.0` |
| `components/newt/component.env` | 更正「旧 site 已失效」的措辞 |
| `components/gatus/endpoints.yaml` | `public-edge` 组按迁移结果重写；`cluster-origin/bugsink` 注释 |
| 本文 + `RECOVERY-STATUS-2026-09-22.md` | 同步新拓扑、迁移对照表、发现的三个坑 |

集群侧动作：补装共享 Gateway、升级 Grafana、部署集群 newt、apply Connect REST NetworkPolicy、
重跑 gatus；node1 升 Pangolin 1.23.0 + gerbil 1.5.2；node0/node2/k1/k2/k3 的 newt 到 1.17.0。

备份位置：`node1:/root/pangolin-backups/`（升级前、迁移前各一份完整 DB），
`k1:/root/grafana-backups/`（升级前 grafana.db）。

### 已知遗留

- `bootstrap/start.sh.orig` 是被提交进仓库的合并残留，本轮没动它，建议单独清理。
- `HOSTING-READINESS-2026-09-03.md` 仍按旧主机名写，属于历史记录，没有改。
- 本轮**没有提交 commit**，工作树里还有其它与本次无关的改动，已按要求保留。
- `bootstrap/scripts/90-verify.sh` 的盲区（只验本轮选中的组件，不扫 live 集群）**没有修**。
  它是这次「共享 Gateway 整个没装却全绿」的直接原因，值得单独做一次。
- k1/k2/k3 三个主机站点当前**没有任何资源**，是为后续节点级 Docker 组件预留的；
  site 显示 online 但资源数为 0 属正常。

## 9. 仓库已改、集群还没跟上的地方

> 2026-09-22 下午更新：原先列在这里的 `gatus` 已重跑，配置已生效。**当前此表为空**
> ——仓库与集群一致。新增改动记得在这里登记，或直接重跑对应 install.sh。

| 组件 | 仓库已改 | 状态 |
|---|---|---|
| `gatus` | public-edge 重写、bugsink 探针注释、PG/观测探针改集群内 | ✅ 已重跑，16 条探针全绿 |
| `grafana` | 换社区 chart 13.2.5、Recreate、requests、root_url 等 | ✅ 已重跑，13.2.2 运行中 |
| `gateway` | 无改动（原本就没装） | ✅ 已补装，VIP `.240` Programmed |
| `newt` | 镜像 1.15.0 → 1.17.0 | ✅ 已部署，site `k8s-cluster` online |
| `opentelemetry` / `vector` | 见上一节 | ✅ 已生效 |

`umami`、`harbor`、`_external/*` 当前都没部署，改动等启用时自然生效。

**另有一条集群改动不在任何 install.sh 里**（重建集群时容易漏）：

```bash
# Strimzi 只放行 operator, 不 apply 这条 gatus 就探不到 Connect REST(表现为 10s 超时)
ssh k1 'export KUBECONFIG=/etc/kubernetes/admin.conf; \
  kubectl apply -f /root/kubernetes/components/kafka/cdc/connect-rest-netpol.yaml'
```

## 10. 常用核对命令

```bash
# 集群
ssh k1 'export KUBECONFIG=/etc/kubernetes/admin.conf; kubectl get nodes; kubectl get pods -A | grep -v Running'
ssh k1 'cilium status'

# 存储
for h in k1 k2 k3; do ssh $h 'vgs openebs-vg'; done

# CNPG + CDC
ssh k1 'export KUBECONFIG=/etc/kubernetes/admin.conf; kubectl -n postgresql get cluster,database'
ssh k1 'export KUBECONFIG=/etc/kubernetes/admin.conf; kubectl -n kafka get kafka,kafkaconnect,kafkaconnector'

# 观测
ssh k1 'export KUBECONFIG=/etc/kubernetes/admin.conf; kubectl -n victoriametrics exec vm-single-victoria-metrics-single-server-0 -- wget -qO- "http://127.0.0.1:8428/api/v1/query?query=count(up)"'
ssh k1 'export KUBECONFIG=/etc/kubernetes/admin.conf; kubectl -n observability exec deploy/vmalert -- wget -qO- "http://alertmanager:9093/api/v2/alerts?active=true"'

# 全局验收
ssh k1 'cd /root/kubernetes/bootstrap && sudo bash start.sh --verify'
```

凭据位置（**不要回显到日志或文档**）：节点 `/root/.k8s-installer-credentials`，
集群内各组件自己的 Secret。ntfy token / topic、newt secret、DB 密码一律只列名字不列值。
